<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bulasuo.art">

    <application
        android:name=".app.App"
        android:icon="@mipmap/ic_launcher_round"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:largeHeap="true"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl,android:name,android:allowBackup">
        <activity android:name=".activity.MainActivity"
            android:theme="@style/XAppWelcomeBg"
            android:alwaysRetainTaskState="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.GZActivityDetail"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activity.GZListActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activity.AppTitleBarBaseWebViewActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>